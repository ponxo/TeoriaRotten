#login
  - if @current_user
    %p.welcome Welcome, #{@current_user.name}!
    = link_to 'Log_Out', logout_path
  - else
    %p.login= link_to 'Log in with your twitter account', '/auth/twitter?force_login=true' 
